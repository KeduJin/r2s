Datamodule:
  kwargs:
    class_name: generation.data.generation_datamodule.GenerationDataModule
    tsv_path: ${PROJ_DIR}/generation/data/0113-retrieval_results.tsv
    num_workers_per_gpu: 8
    per_gpu_batchsize: 64
    eval_batch_size: 64
